# Arquivo opcional de customização para nginx-proxy.
# Copie para /etc/nginx/vhost.d/srv1655483.hstgr.cloud dentro do container do proxy
# caso queira overrides por vhost.

client_max_body_size 10m;
proxy_read_timeout 60s;
proxy_connect_timeout 15s;
proxy_send_timeout 60s;
